c_compiler_version:
  - if: linux
    then: "15"
gcc_compiler_version:
  - if: linux
    then: "15"
cxx_compiler_version:
  - if: linux
    then: "15"
gxx_compiler_version:
  - if: linux
    then: "15"
c_stdlib_version:
  - if: osx  # macOS/libc++ older than 15.0 may not support std::chrono::current_zone()
    then: "15.0"
